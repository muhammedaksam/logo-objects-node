/**
 * @module BankSlips
 */

import { BaseApiClient } from '../base';
import { ApiResponse, ApiClientConfig, RequestQueryBodyParameter } from '../../types';
import { buildSearchQuery } from '../../utils/queryBuilder';
import {
  BankSlips,
  BankSlipsSearchCriteria,
  BankSlipsAnalytics,
  KeyValueParameter,
  RsPaylstxml,
  ResultData,
  ExtendedFieldDefinitions,
  ChequeAndPnoteRolls,
  RsBankcrepayxml,
  RsBanktransxml,
  RsDiscpaytrxml,
  RsPreaccdistlnxml,
  RsArptransxml,
  RsChqpntransxml,
  RsChqpnstatxml,
  Arps,
  PurchaseInvoices,
  RsArpnotesxml,
  RsOkcinfoxml,
  RsLabelxml,
  RsIntellistxml,
  PurchasedServices,
  RsAddtaxmultixml,
  PurchaseDiscounts,
  RsDesptransxml,
  RsServunitasgn,
  RsGlpostxml,
  Items,
  RsSrvinvenxml,
  RsVrntcdtempvaluex,
  RsVrntcdtemplatexm,
  ItemAlternatives,
  RsExclistnodexml,
  RsExclistnodevalxm,
  RsItemqprodxml,
  RsItemunitasgn,
  RsItemcmpxml,
  RsItemsuppxml,
  RsBarcodexml,
  RsItemchcodesxml,
  RsItemchvaluesxml,
  RsXmldomcls,
  RsItemfactoryxml,
  RsIteminvenxml,
  RsQccvallistxml,
  RsQccvalentryxml,
  RsCampcodeslistxml,
  RsAddtaxmultilinex,
  RsSlloctrnxml,
  RsFaregtrnxml,
  RsDetmattrxml,
  PurchaseDispatches,
  BankSlipsQueryOptions,
} from './types';

/**
 * @class BankSlipsClient
 * @extends BaseApiClient
 * @description
 * The `BankSlipsClient` provides an interface for interacting with the `BankSlips` entities of the Logo Objects API.
 * This client includes methods for all common CRUD operations, as well as specialized methods for searching and analytics.
 *
 * @see {@link https://polaris.logo.cloud/docs/tiger-uyarlama-araci/detail/external%253Fcid%253Dc870583a-5e52-44e0-a311-a575a18a6088%2526link%253Df3c988fb-cd15-4596-ba1d-0b78376a530e%2526tenantId%253Dcdd87e13-3009-4dd1-a5b8-2a005c0e58da%2526hideName%253DTrue|Logo Objects REST Service} for more details.
 *
 * @category Finans
 * @category Hareketler
 * @category Banka Fişleri
 * @category Alan Özellikleri
 */
export class BankSlipsClient extends BaseApiClient {
  private readonly endpoint = '/bankSlips';

  /**
   * @constructor
   * @param {ApiClientConfig} config - The configuration for the API client.
   */
  constructor(config: ApiClientConfig) {
    super(config);
  }

  /**
   * @method getAll
   * @description Retrieves a paginated list of `BankSlips` entities.
   * @param {BankSlipsQueryOptions} [options] - Optional parameters for filtering, sorting, and pagination with type-safe field names.
   * @returns {Promise<ApiResponse<BankSlips>>} A promise that resolves to a paginated API response of `BankSlips` entities.
   *
   * @example
   * // Retrieves the first 10 BankSlips entities with specific fields
   * const bankSlips = await client.bankSlips.getAll({
   *   limit: 10,
   *   fields: ['CODE', 'TITLE', 'STATUS'],  // ✨ Full IntelliSense for field names
   *   sort: [['CODE', 'DATE_CREATED'], 'desc']  // ✨ Type-safe sort with direction
   * });
   */
  async getAll(options?: BankSlipsQueryOptions): Promise<ApiResponse<BankSlips>> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request('get', `${this.endpoint}${queryString ? `?${queryString}` : ''}`);
  }

  /**
   * @method getById
   * @description Retrieves a single `BankSlips` entity by its internal reference ID.
   * @param {number} id - The internal reference ID of the `BankSlips` entity.
   * @param {BankSlipsQueryOptions} [options] - Optional parameters for expanding related entities or selecting specific fields.
   * @returns {Promise<BankSlips>} A promise that resolves to the requested `BankSlips` entity.
   *
   * @example
   * // Retrieves a BankSlips with ID 123 with specific fields
   * const bankSlips = await client.bankSlips.getById(123, {
   *   fields: ['CODE', 'TITLE', 'STATUS']  // ✨ Full IntelliSense for field names
   * });
   */
  async getById(id: number, options?: BankSlipsQueryOptions): Promise<BankSlips> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request('get', `${this.endpoint}/${id}${queryString ? `?${queryString}` : ''}`);
  }

  /**
   * @method create
   * @description Creates a new `BankSlips` entity.
   * @param {Partial<BankSlips>} data - The data for the new `BankSlips` entity.
   * @returns {Promise<BankSlips>} A promise that resolves to the newly created `BankSlips` entity.
   *
   * @example
   * // Creates a new BankSlips
   * const newBankSlips = await client.bankSlips.create({
   *   name: 'New BankSlips',
   *   // ... other properties
   * });
   */
  async create(data: Partial<BankSlips>): Promise<BankSlips> {
    return this.request('post', this.endpoint, data);
  }

  /**
   * @method update
   * @description Updates an existing `BankSlips` entity.
   * @param {number} id - The internal reference ID of the `BankSlips` entity to update.
   * @param {Partial<BankSlips>} data - The updated data for the `BankSlips` entity.
   * @returns {Promise<BankSlips>} A promise that resolves to the updated `BankSlips` entity.
   *
   * @example
   * // Updates the name of a BankSlips with ID 123
   * const updatedBankSlips = await client.bankSlips.update(123, {
   *   name: 'Updated BankSlips Name',
   * });
   */
  async update(id: number, data: Partial<BankSlips>): Promise<BankSlips> {
    return this.request('put', `${this.endpoint}/${id}`, data);
  }

  /**
   * @method patch
   * @description Partially updates a `BankSlips` entity.
   * @param {number} id - The internal reference ID of the `BankSlips` entity to update.
   * @param {Partial<BankSlips>} data - The partial data to update.
   * @returns {Promise<BankSlips>} A promise that resolves to the updated `BankSlips` entity.
   *
   * @example
   * // Partially updates a BankSlips with ID 123
   * const patchedBankSlips = await client.bankSlips.patch(123, {
   *   status: 'inactive',
   * });
   */
  async patch(id: number, data: Partial<BankSlips>): Promise<BankSlips> {
    return this.request('patch', `${this.endpoint}/${id}`, data);
  }

  /**
   * @method delete
   * @description Deletes a `BankSlips` entity by its internal reference ID.
   * @param {number} id - The internal reference ID of the `BankSlips` entity to delete.
   * @returns {Promise<BankSlips>} A promise that resolves when the entity is successfully deleted.
   *
   * @example
   * // Deletes a BankSlips with ID 123
   * await client.bankSlips.delete(123);
   */
  async delete(id: number): Promise<BankSlips> {
    return this.request('delete', `${this.endpoint}/${id}`);
  }

  /**
   * @method search
   * @description Searches for `BankSlips` entities based on a set of criteria.
   * @param {BankSlipsSearchCriteria} criteria - The search criteria.
   * @returns {Promise<ApiResponse<BankSlips>>} A promise that resolves to a paginated API response of matching `BankSlips` entities.
   *
   * @example
   * // Searches for BankSlips entities with a specific code
   * const results = await client.bankSlips.search({ code: '123' });
   */
  async search(criteria: BankSlipsSearchCriteria): Promise<ApiResponse<BankSlips>> {
    const query = this.buildSearchQuery(criteria);
    return this.getAll({ q: query });
  }

  /**
   * @method buildQuery
   * @description Builds a custom query string for advanced filtering.
   * @param {string[]} conditions - An array of OData query conditions.
   * @returns {Promise<ApiResponse<BankSlips>>} A promise that resolves to a paginated API response of matching `BankSlips` entities.
   *
   * @example
   * // Builds a complex query
   * const results = await client.bankSlips.buildQuery([
   *   "name like '%test%'",
   *   "status eq 'active'"
   * ]);
   */
  async buildQuery(conditions: string[]): Promise<ApiResponse<BankSlips>> {
    const query = conditions.join(' and ');
    return this.getAll({ q: query });
  }

  /**
   * @method equalizePayAmntV1
   * @description Corresponds to `POST /bankSlips/EqualizePayAmnt`
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async equalizePayAmntV1(data: Partial<BankSlips>): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/EqualizePayAmnt`, data);
  }

  /**
   * @method equalizePayAmntV1Get
   * @description Corresponds to `GET /bankSlips/{id}/EqualizePayAmnt`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async equalizePayAmntV1Get(
    id: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/EqualizePayAmnt${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method addSeriLotsForKsV1
   * @description Corresponds to `GET /bankSlips/{id}/AddSeriLotsForKs/{_slCode}/{_amount}/{_IOMtd}`
   * @param {number} id - id
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} IOMtd - _IOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async addSeriLotsForKsV1(
    id: number,
    slCode: string,
    amount: number,
    IOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/AddSeriLotsForKs/${slCode}/${amount}/${IOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method addSeriLotsForKsV1Post
   * @description Corresponds to `POST /bankSlips/AddSeriLotsForKs/{_slCode}/{_amount}/{_IOMtd}`
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} IOMtd - _IOMtd
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async addSeriLotsForKsV1Post(
    slCode: string,
    amount: number,
    IOMtd: number,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/AddSeriLotsForKs/${slCode}/${amount}/${IOMtd}`, data);
  }

  /**
   * @method fillSMMACCCodesV1
   * @description Corresponds to `GET /bankSlips/{id}/FillSMMACCCodes`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async fillSMMACCCodesV1(
    id: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/FillSMMACCCodes${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method fillSMMACCCodesV1Post
   * @description Corresponds to `POST /bankSlips/FillSMMACCCodes`
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async fillSMMACCCodesV1Post(data: Partial<BankSlips>): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/FillSMMACCCodes`, data);
  }

  /**
   * @method attachADespatchByLRefV1
   * @description Corresponds to `GET /bankSlips/{id}/AttachADespatchByLRef/{_dispref}`
   * @param {number} id - id
   * @param {number} dispref - _dispref
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachADespatchByLRefV1(
    id: number,
    dispref: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/AttachADespatchByLRef/${dispref}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachADespatchByLRefV1Post
   * @description Corresponds to `POST /bankSlips/AttachADespatchByLRef/{_dispref}`
   * @param {number} dispref - _dispref
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachADespatchByLRefV1Post(
    dispref: number,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/AttachADespatchByLRef/${dispref}`, data);
  }

  /**
   * @method attachADespatchByFicheNoV1
   * @description Corresponds to `GET /bankSlips/{id}/AttachADespatchByFicheNo/{_dispFicheNo}`
   * @param {number} id - id
   * @param {string} dispFicheNo - _dispFicheNo
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachADespatchByFicheNoV1(
    id: number,
    dispFicheNo: string,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/AttachADespatchByFicheNo/${dispFicheNo}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachADespatchByFicheNoV1Post
   * @description Corresponds to `POST /bankSlips/AttachADespatchByFicheNo/{_dispFicheNo}`
   * @param {string} dispFicheNo - _dispFicheNo
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachADespatchByFicheNoV1Post(
    dispFicheNo: string,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/AttachADespatchByFicheNo/${dispFicheNo}`, data);
  }

  /**
   * @method deleteCampaignV1
   * @description Corresponds to `GET /bankSlips/{id}/DeleteCampaign`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async deleteCampaignV1(
    id: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/DeleteCampaign${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method deleteCampaignV1Post
   * @description Corresponds to `POST /bankSlips/DeleteCampaign`
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async deleteCampaignV1Post(data: Partial<BankSlips>): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/DeleteCampaign`, data);
  }

  /**
   * @method setClientInfoV1
   * @description Corresponds to `GET /bankSlips/{id}/SetClientInfo`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async setClientInfoV1(id: number, options?: BankSlipsQueryOptions): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/SetClientInfo${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method setClientInfoV1Post
   * @description Corresponds to `POST /bankSlips/SetClientInfo`
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async setClientInfoV1Post(data: Partial<BankSlips>): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/SetClientInfo`, data);
  }

  /**
   * @method getRelevantCampaignsV1
   * @description Corresponds to `GET /bankSlips/{id}/GetRelevantCampaigns`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async getRelevantCampaignsV1(
    id: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/GetRelevantCampaigns${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getRelevantCampaignsV1Post
   * @description Corresponds to `POST /bankSlips/GetRelevantCampaigns`
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async getRelevantCampaignsV1Post(data: Partial<BankSlips>): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/GetRelevantCampaigns`, data);
  }

  /**
   * @method applyCampaignSpecificV1
   * @description Corresponds to `GET /bankSlips/{id}/ApplyCampaignSpecific/{_refList}`
   * @param {number} id - id
   * @param {string} refList - _refList
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async applyCampaignSpecificV1(
    id: number,
    refList: string,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ApplyCampaignSpecific/${refList}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method applyCampaignSpecificV1Post
   * @description Corresponds to `POST /bankSlips/ApplyCampaignSpecific/{_refList}`
   * @param {string} refList - _refList
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async applyCampaignSpecificV1Post(
    refList: string,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/ApplyCampaignSpecific/${refList}`, data);
  }

  /**
   * @method importImageV1
   * @description Corresponds to `GET /bankSlips/{id}/ImportImage/{_ImgPath}/{_Indx}`
   * @param {number} id - id
   * @param {string} ImgPath - _ImgPath
   * @param {string} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async importImageV1(
    id: number,
    ImgPath: string,
    Indx: string,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ImportImage/${ImgPath}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method importImageV1Post
   * @description Corresponds to `POST /bankSlips/ImportImage/{_ImgPath}/{_Indx}`
   * @param {string} ImgPath - _ImgPath
   * @param {string} Indx - _Indx
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async importImageV1Post(
    ImgPath: string,
    Indx: string,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/ImportImage/${ImgPath}/${Indx}`, data);
  }

  /**
   * @method exportBase64EncodedImageV1
   * @description Corresponds to `GET /bankSlips/{id}/ExportBase64EncodedImage/{_Indx}`
   * @param {number} id - id
   * @param {string} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async exportBase64EncodedImageV1(
    id: number,
    Indx: string,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ExportBase64EncodedImage/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method exportBase64EncodedImageV1Post
   * @description Corresponds to `POST /bankSlips/ExportBase64EncodedImage/{_Indx}`
   * @param {string} Indx - _Indx
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async exportBase64EncodedImageV1Post(
    Indx: string,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/ExportBase64EncodedImage/${Indx}`, data);
  }

  /**
   * @method importBase64EncodedImageV1
   * @description Corresponds to `GET /bankSlips/{id}/ImportBase64EncodedImage/{_base64EncodedImage}/{_imageType}/{_Indx}`
   * @param {number} id - id
   * @param {string} base64EncodedImage - _base64EncodedImage
   * @param {string} imageType - _imageType
   * @param {string} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async importBase64EncodedImageV1(
    id: number,
    base64EncodedImage: string,
    imageType: string,
    Indx: string,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ImportBase64EncodedImage/${base64EncodedImage}/${imageType}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method importBase64EncodedImageV1Post
   * @description Corresponds to `POST /bankSlips/ImportBase64EncodedImage/{_base64EncodedImage}/{_imageType}/{_Indx}`
   * @param {string} base64EncodedImage - _base64EncodedImage
   * @param {string} imageType - _imageType
   * @param {string} Indx - _Indx
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async importBase64EncodedImageV1Post(
    base64EncodedImage: string,
    imageType: string,
    Indx: string,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request(
      'post',
      `/bankSlips/ImportBase64EncodedImage/${base64EncodedImage}/${imageType}/${Indx}`,
      data
    );
  }

  /**
   * @method defnfldslistGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistGetstocklinepriceV1(
    prcTyp: number,
    Indx: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method defnfldslistGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistGetstocklinepriceV1Get(
    id: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method defnfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistAddline2V1(
    id: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method defnfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} index - _index
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistAddline2V1Post(
    index: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method defnfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistAppendserilotsV1(
    id: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method defnfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistAppendserilotsV1Post(
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method defnfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistEqualizebalanceV1(
    id: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method defnfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} index - _index
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistEqualizebalanceV1Post(
    index: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method defnfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistApplyaccdisttemplateV1(
    id: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method defnfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistApplyaccdisttemplateV1Post(
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method defnfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistSetlinetotalsV1(
    id: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method defnfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} Indx - _Indx
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistSetlinetotalsV1Post(
    Indx: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method exportToXMLV1
   * @description Corresponds to `GET /bankSlips/{id}/ExportToXML/{_RootKey}/{_FileName}`
   * @param {number} id - id
   * @param {string} RootKey - _RootKey
   * @param {string} FileName - _FileName
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async exportToXMLV1(
    id: number,
    RootKey: string,
    FileName: string,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ExportToXML/${RootKey}/${FileName}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method exportToXMLV1Post
   * @description Corresponds to `POST /bankSlips/ExportToXML/{_RootKey}/{_FileName}`
   * @param {string} RootKey - _RootKey
   * @param {string} FileName - _FileName
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async exportToXMLV1Post(
    RootKey: string,
    FileName: string,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/ExportToXML/${RootKey}/${FileName}`, data);
  }

  /**
   * @method importFromXMLFileV1
   * @description Corresponds to `GET /bankSlips/{id}/ImportFromXMLFile/{_RootKey}/{_FileName}`
   * @param {number} id - id
   * @param {string} RootKey - _RootKey
   * @param {string} FileName - _FileName
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async importFromXMLFileV1(
    id: number,
    RootKey: string,
    FileName: string,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ImportFromXMLFile/${RootKey}/${FileName}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method importFromXMLFileV1Post
   * @description Corresponds to `POST /bankSlips/ImportFromXMLFile/{_RootKey}/{_FileName}`
   * @param {string} RootKey - _RootKey
   * @param {string} FileName - _FileName
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async importFromXMLFileV1Post(
    RootKey: string,
    FileName: string,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/ImportFromXMLFile/${RootKey}/${FileName}`, data);
  }

  /**
   * @method createCompositeLinesV1
   * @description Corresponds to `GET /bankSlips/{id}/CreateCompositeLines`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async createCompositeLinesV1(
    id: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/CreateCompositeLines${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method createCompositeLinesV1Post
   * @description Corresponds to `POST /bankSlips/CreateCompositeLines`
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async createCompositeLinesV1Post(data: Partial<BankSlips>): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/CreateCompositeLines`, data);
  }

  /**
   * @method formSeriLotLinesV1
   * @description Corresponds to `GET /bankSlips/{id}/FormSeriLotLines/{_lOMtd}`
   * @param {number} id - id
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async formSeriLotLinesV1(
    id: number,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/FormSeriLotLines/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method formSeriLotLinesV1Post
   * @description Corresponds to `POST /bankSlips/FormSeriLotLines/{_lOMtd}`
   * @param {number} lOMtd - _lOMtd
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async formSeriLotLinesV1Post(
    lOMtd: number,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/FormSeriLotLines/${lOMtd}`, data);
  }

  /**
   * @method applyCampaignV1
   * @description Corresponds to `GET /bankSlips/{id}/ApplyCampaign`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async applyCampaignV1(id: number, options?: BankSlipsQueryOptions): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ApplyCampaign${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method applyCampaignV1Post
   * @description Corresponds to `POST /bankSlips/ApplyCampaign`
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async applyCampaignV1Post(data: Partial<BankSlips>): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/ApplyCampaign`, data);
  }

  /**
   * @method applyRePayPlnV1
   * @description Corresponds to `GET /bankSlips/{id}/ApplyRePayPln/{_lineNr}/{_rePayPCode}`
   * @param {number} id - id
   * @param {number} lineNr - _lineNr
   * @param {string} rePayPCode - _rePayPCode
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async applyRePayPlnV1(
    id: number,
    lineNr: number,
    rePayPCode: string,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ApplyRePayPln/${lineNr}/${rePayPCode}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method applyRePayPlnV1Post
   * @description Corresponds to `POST /bankSlips/ApplyRePayPln/{_lineNr}/{_rePayPCode}`
   * @param {number} lineNr - _lineNr
   * @param {string} rePayPCode - _rePayPCode
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async applyRePayPlnV1Post(
    lineNr: number,
    rePayPCode: string,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/ApplyRePayPln/${lineNr}/${rePayPCode}`, data);
  }

  /**
   * @method applyRePayPlnForInvV1
   * @description Corresponds to `GET /bankSlips/{id}/ApplyRePayPlnForInv/{_rePayCode}`
   * @param {number} id - id
   * @param {string} rePayCode - _rePayCode
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async applyRePayPlnForInvV1(
    id: number,
    rePayCode: string,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ApplyRePayPlnForInv/${rePayCode}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method applyRePayPlnForInvV1Post
   * @description Corresponds to `POST /bankSlips/ApplyRePayPlnForInv/{_rePayCode}`
   * @param {string} rePayCode - _rePayCode
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async applyRePayPlnForInvV1Post(
    rePayCode: string,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/ApplyRePayPlnForInv/${rePayCode}`, data);
  }

  /**
   * @method importFromXmlStrV1
   * @description Corresponds to `GET /bankSlips/{id}/ImportFromXmlStr/{_RootKey}/{_XmlStr}`
   * @param {number} id - id
   * @param {string} RootKey - _RootKey
   * @param {string} XmlStr - _XmlStr
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async importFromXmlStrV1(
    id: number,
    RootKey: string,
    XmlStr: string,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ImportFromXmlStr/${RootKey}/${XmlStr}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method importFromXmlStrV1Post
   * @description Corresponds to `POST /bankSlips/ImportFromXmlStr/{_RootKey}/{_XmlStr}`
   * @param {string} RootKey - _RootKey
   * @param {string} XmlStr - _XmlStr
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async importFromXmlStrV1Post(
    RootKey: string,
    XmlStr: string,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/ImportFromXmlStr/${RootKey}/${XmlStr}`, data);
  }

  /**
   * @method exportToXmlStrV1
   * @description Corresponds to `GET /bankSlips/{id}/ExportToXmlStr/{_RootKey}`
   * @param {number} id - id
   * @param {string} RootKey - _RootKey
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async exportToXmlStrV1(
    id: number,
    RootKey: string,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ExportToXmlStr/${RootKey}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method exportToXmlStrV1Post
   * @description Corresponds to `POST /bankSlips/ExportToXmlStr/{_RootKey}`
   * @param {string} RootKey - _RootKey
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async exportToXmlStrV1Post(
    RootKey: string,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/ExportToXmlStr/${RootKey}`, data);
  }

  /**
   * @method applyConditionV1
   * @description Corresponds to `GET /bankSlips/{id}/ApplyCondition`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async applyConditionV1(
    id: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ApplyCondition${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method applyConditionV1Post
   * @description Corresponds to `POST /bankSlips/ApplyCondition`
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async applyConditionV1Post(data: Partial<BankSlips>): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/ApplyCondition`, data);
  }

  /**
   * @method exportImageV1
   * @description Corresponds to `GET /bankSlips/{id}/ExportImage/{_Indx}/{_ImgPath}`
   * @param {number} id - id
   * @param {string} Indx - _Indx
   * @param {string} ImgPath - _ImgPath
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async exportImageV1(
    id: number,
    Indx: string,
    ImgPath: string,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ExportImage/${Indx}/${ImgPath}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method exportImageV1Post
   * @description Corresponds to `POST /bankSlips/ExportImage/{_Indx}/{_ImgPath}`
   * @param {string} Indx - _Indx
   * @param {string} ImgPath - _ImgPath
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async exportImageV1Post(
    Indx: string,
    ImgPath: string,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/ExportImage/${Indx}/${ImgPath}`, data);
  }

  /**
   * @method reCalculateV1
   * @description Corresponds to `GET /bankSlips/{id}/ReCalculate`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async reCalculateV1(id: number, options?: BankSlipsQueryOptions): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ReCalculate${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method reCalculateV1Post
   * @description Corresponds to `POST /bankSlips/ReCalculate`
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async reCalculateV1Post(data: Partial<BankSlips>): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/ReCalculate`, data);
  }

  /**
   * @method fillAccCodesV1
   * @description Corresponds to `GET /bankSlips/{id}/FillAccCodes`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async fillAccCodesV1(id: number, options?: BankSlipsQueryOptions): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/FillAccCodes${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method fillAccCodesV1Post
   * @description Corresponds to `POST /bankSlips/FillAccCodes`
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async fillAccCodesV1Post(data: Partial<BankSlips>): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/FillAccCodes`, data);
  }

  /**
   * @method applyADiscountV1
   * @description Corresponds to `GET /bankSlips/{id}/ApplyADiscount/{_discCode}`
   * @param {number} id - id
   * @param {string} discCode - _discCode
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async applyADiscountV1(
    id: number,
    discCode: string,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ApplyADiscount/${discCode}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method applyADiscountV1Post
   * @description Corresponds to `POST /bankSlips/ApplyADiscount/{_discCode}`
   * @param {string} discCode - _discCode
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async applyADiscountV1Post(
    discCode: string,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request('post', `/bankSlips/ApplyADiscount/${discCode}`, data);
  }

  /**
   * @method setDefIntValueV1
   * @description Corresponds to `GET /bankSlips/{id}/SetDefIntValue/{_fldName}/{_fType}/{_fSz}/{_Value}`
   * @param {number} id - id
   * @param {string} fldName - _fldName
   * @param {number} fType - _fType
   * @param {number} fSz - _fSz
   * @param {number} Value - _Value
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async setDefIntValueV1(
    id: number,
    fldName: string,
    fType: number,
    fSz: number,
    Value: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/SetDefIntValue/${fldName}/${fType}/${fSz}/${Value}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method setDefIntValueV1Post
   * @description Corresponds to `POST /bankSlips/SetDefIntValue/{_fldName}/{_fType}/{_fSz}/{_Value}`
   * @param {string} fldName - _fldName
   * @param {number} fType - _fType
   * @param {number} fSz - _fSz
   * @param {number} Value - _Value
   * @param {Partial<BankSlips>} data - The request body.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async setDefIntValueV1Post(
    fldName: string,
    fType: number,
    fSz: number,
    Value: number,
    data: Partial<BankSlips>
  ): Promise<KeyValueParameter[]> {
    return this.request(
      'post',
      `/bankSlips/SetDefIntValue/${fldName}/${fType}/${fSz}/${Value}`,
      data
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLDEFNFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLDEFNFLDSLISTV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLDEFNFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLDEFNFLDSLISTByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolldefnfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolldefnfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistAppendline2V1Post(
    id1: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolldefnfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolldefnfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolldefnfldslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistGetstocklinepriceV1(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolldefnfldslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistGetstocklinepriceV1Post(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolldefnfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolldefnfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistAddline2V1Post(
    id1: number,
    index: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolldefnfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolldefnfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolldefnfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolldefnfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolldefnfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolldefnfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistApplyaccdisttemplateV1Post(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolldefnfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolldefnfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolldefnfldslistSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getBNCREPAYMENTLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/BNCREPAYMENTLIST`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getBNCREPAYMENTLISTV1(id: number, options?: BankSlipsQueryOptions): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/BNCREPAYMENTLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getBNCREPAYMENTLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/BNCREPAYMENTLIST/{idt}`
   * @param {number} id - id
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsBankcrepayxml>} A promise resolving to the response.
   */
  async getBNCREPAYMENTLISTByIdV1(
    id: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsBankcrepayxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/BNCREPAYMENTLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method bncrepaymentlistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/BNCREPAYMENTLIST/AppendLine2`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistAppendline2V1(
    id: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/BNCREPAYMENTLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method bncrepaymentlistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/BNCREPAYMENTLIST/AppendLine2`
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistAppendline2V1Post(
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/BNCREPAYMENTLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method bncrepaymentlistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/BNCREPAYMENTLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistAddserilotsV1(
    id: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/BNCREPAYMENTLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method bncrepaymentlistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/BNCREPAYMENTLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistAddserilotsV1Post(
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/BNCREPAYMENTLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method bncrepaymentlistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/BNCREPAYMENTLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistGetstocklinepriceV1(
    id: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/BNCREPAYMENTLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method bncrepaymentlistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/BNCREPAYMENTLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistGetstocklinepriceV1Post(
    prcTyp: number,
    Indx: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/BNCREPAYMENTLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method bncrepaymentlistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/BNCREPAYMENTLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistAddline2V1(
    id: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/BNCREPAYMENTLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method bncrepaymentlistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/BNCREPAYMENTLIST/AddLine2/{_index}`
   * @param {number} index - _index
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistAddline2V1Post(
    index: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/BNCREPAYMENTLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method bncrepaymentlistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/BNCREPAYMENTLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistAppendserilotsV1(
    id: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/BNCREPAYMENTLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method bncrepaymentlistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/BNCREPAYMENTLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistAppendserilotsV1Post(
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/BNCREPAYMENTLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method bncrepaymentlistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/BNCREPAYMENTLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistEqualizebalanceV1(
    id: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/BNCREPAYMENTLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method bncrepaymentlistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/BNCREPAYMENTLIST/EqualizeBalance/{_index}`
   * @param {number} index - _index
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistEqualizebalanceV1Post(
    index: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/BNCREPAYMENTLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method bncrepaymentlistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/BNCREPAYMENTLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistApplyaccdisttemplateV1(
    id: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/BNCREPAYMENTLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method bncrepaymentlistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/BNCREPAYMENTLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistApplyaccdisttemplateV1Post(
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/BNCREPAYMENTLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method bncrepaymentlistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/BNCREPAYMENTLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistSetlinetotalsV1(
    id: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/BNCREPAYMENTLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method bncrepaymentlistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/BNCREPAYMENTLIST/SetLineTotals/{_Indx}`
   * @param {number} Indx - _Indx
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async bncrepaymentlistSetlinetotalsV1Post(
    Indx: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/BNCREPAYMENTLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getDEFNFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/DEFNFLDSLIST`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getDEFNFLDSLISTV1(id: number, options?: BankSlipsQueryOptions): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/DEFNFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getDEFNFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/DEFNFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getDEFNFLDSLISTByIdV1(
    id: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/DEFNFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method defnfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistAppendline2V1(
    id: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method defnfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/DEFNFLDSLIST/AppendLine2`
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistAppendline2V1Post(
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method defnfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistAddserilotsV1(
    id: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method defnfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async defnfldslistAddserilotsV1Post(
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesGetstocklinepriceV1(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLPAYMENT_LISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLPAYMENT_LISTV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLPAYMENT_LISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsPaylstxml>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLPAYMENT_LISTByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsPaylstxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListAppendline2V1Post(
    id1: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollpaymentListAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollpaymentListGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListGetstocklinepriceV1(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListGetstocklinepriceV1Post(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollpaymentListAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListAddline2V1Post(
    id1: number,
    index: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollpaymentListAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollpaymentListEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollpaymentListApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListApplyaccdisttemplateV1(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListApplyaccdisttemplateV1Post(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollpaymentListSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLPAYMENT_LISTDISCTRLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLPAYMENT_LISTDISCTRLISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLPAYMENT_LISTDISCTRLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsDiscpaytrxml>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLPAYMENT_LISTDISCTRLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsDiscpaytrxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollpaymentListdisctrlistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollpaymentListdisctrlistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLBANK_TRANSACTIONSPAYMENT_LISTDISCTRLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLBANK_TRANSACTIONSPAYMENT_LISTDISCTRLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLBANK_TRANSACTIONSPAYMENT_LISTDISCTRLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsDiscpaytrxml>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLBANK_TRANSACTIONSPAYMENT_LISTDISCTRLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsDiscpaytrxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListdisctrlistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListdisctrlistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLBANK_TRANSACTIONSDEFNFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLBANK_TRANSACTIONSDEFNFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLBANK_TRANSACTIONSDEFNFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLBANK_TRANSACTIONSDEFNFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsdefnfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsdefnfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLBANK_TRANSACTIONSPREACCLINESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLBANK_TRANSACTIONSPREACCLINESV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLBANK_TRANSACTIONSPREACCLINESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsPreaccdistlnxml>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLBANK_TRANSACTIONSPREACCLINESByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsPreaccdistlnxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspreacclinesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PREACCLINES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspreacclinesAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PREACCLINES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesGetstocklinepriceV1(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLBANK_TRANSACTIONSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLBANK_TRANSACTIONSV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLBANK_TRANSACTIONSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsBanktransxml>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLBANK_TRANSACTIONSByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsBanktransxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/AppendLine2`
   * @param {number} id1 - id1
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsAppendline2V1Post(
    id1: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsGetstocklinepriceV1(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsGetstocklinepriceV1Post(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsAddline2V1Post(
    id1: number,
    index: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsApplyaccdisttemplateV1Post(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionsSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLBANK_TRANSACTIONSPAYMENT_LISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLBANK_TRANSACTIONSPAYMENT_LISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLBANK_TRANSACTIONSPAYMENT_LISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsPaylstxml>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLBANK_TRANSACTIONSPAYMENT_LISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsPaylstxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollbankTransactionspaymentListEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/BANK_TRANSACTIONS/{id2}/PAYMENT_LIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollbankTransactionspaymentListEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/BANK_TRANSACTIONS/${id2}/PAYMENT_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLARP_TRANSACTIONSPAYMENT_LISTDISCTRLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLARP_TRANSACTIONSPAYMENT_LISTDISCTRLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLARP_TRANSACTIONSPAYMENT_LISTDISCTRLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsDiscpaytrxml>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLARP_TRANSACTIONSPAYMENT_LISTDISCTRLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsDiscpaytrxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListdisctrlistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{id3}/DISCTRLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListdisctrlistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${id3}/DISCTRLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLARP_TRANSACTIONSDEFNFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLARP_TRANSACTIONSDEFNFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLARP_TRANSACTIONSDEFNFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLARP_TRANSACTIONSDEFNFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsdefnfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsdefnfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLARP_TRANSACTIONSPREACCLINESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLARP_TRANSACTIONSPREACCLINESV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLARP_TRANSACTIONSPREACCLINESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsPreaccdistlnxml>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLARP_TRANSACTIONSPREACCLINESByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsPreaccdistlnxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspreacclinesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PREACCLINES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspreacclinesAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PREACCLINES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistGetstocklinepriceV1(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLARP_TRANSACTIONSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLARP_TRANSACTIONSV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLARP_TRANSACTIONSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsArptransxml>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLARP_TRANSACTIONSByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsArptransxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/AppendLine2`
   * @param {number} id1 - id1
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsAppendline2V1Post(
    id1: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsGetstocklinepriceV1(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsGetstocklinepriceV1Post(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsAddline2V1Post(
    id1: number,
    index: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsApplyaccdisttemplateV1Post(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionsSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLARP_TRANSACTIONSPAYMENT_LISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLARP_TRANSACTIONSPAYMENT_LISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLARP_TRANSACTIONSPAYMENT_LISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsPaylstxml>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLARP_TRANSACTIONSPAYMENT_LISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsPaylstxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollarpTransactionspaymentListEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/ARP_TRANSACTIONS/{id2}/PAYMENT_LIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsArptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollarpTransactionspaymentListEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsArptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/ARP_TRANSACTIONS/${id2}/PAYMENT_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollApplyaccdisttemplateV1(
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollApplyaccdisttemplateV1Get(
    id: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollSetlinetotalsV1(
    id: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/SetLineTotals/{_Indx}`
   * @param {number} Indx - _Indx
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollSetlinetotalsV1Post(
    Indx: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLTRANSACTIONSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLTRANSACTIONSV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLTRANSACTIONSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsChqpntransxml>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLTRANSACTIONSByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsChqpntransxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/AppendLine2`
   * @param {number} id1 - id1
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsAppendline2V1Post(
    id1: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsGetstocklinepriceV1(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsGetstocklinepriceV1Post(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsAddline2V1Post(
    id1: number,
    index: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsApplyaccdisttemplateV1Post(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {ChequeAndPnoteRolls} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: ChequeAndPnoteRolls,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLTRANSACTIONSSTATUS_LISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLTRANSACTIONSSTATUS_LISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLTRANSACTIONSSTATUS_LISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsChqpnstatxml>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLTRANSACTIONSSTATUS_LISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsChqpnstatxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsstatusListSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/STATUS_LIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsstatusListSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/STATUS_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLTRANSACTIONSDEFNFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLTRANSACTIONSDEFNFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLTRANSACTIONSDEFNFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLTRANSACTIONSDEFNFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRolltransactionsdefnfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsChqpntransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRolltransactionsdefnfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsChqpntransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistGetstocklinepriceV1(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEXRELATED_ARPDEFNFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEXRELATED_ARPDEFNFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEXRELATED_ARPDEFNFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEXRELATED_ARPDEFNFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpdefnfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpdefnfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_ROLLV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLV1(id: number, options?: BankSlipsQueryOptions): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_ROLLByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/{idt}`
   * @param {number} id - id
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ChequeAndPnoteRolls>} A promise resolving to the response.
   */
  async getATTACHMENT_ROLLByIdV1(
    id: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ChequeAndPnoteRolls> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/AppendLine2`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollAppendline2V1(
    id: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/AppendLine2`
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollAppendline2V1Post(
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollAddserilotsV1(
    id: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollAddserilotsV1Post(
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollGetstocklinepriceV1(
    id: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollGetstocklinepriceV1Post(
    prcTyp: number,
    Indx: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollAddline2V1(
    id: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/AddLine2/{_index}`
   * @param {number} index - _index
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollAddline2V1Post(
    index: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollAppendserilotsV1(
    id: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollAppendserilotsV1Post(
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentRollEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_ROLL/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollEqualizebalanceV1(
    id: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_ROLL/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentRollEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_ROLL/EqualizeBalance/{_index}`
   * @param {number} index - _index
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentRollEqualizebalanceV1Post(
    index: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_ROLL/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicelabelListApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListApplyaccdisttemplateV1(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicelabelListApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicelabelListSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicelabelListSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEXRELATED_ARPV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEXRELATED_ARPV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEXRELATED_ARPByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<Arps>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEXRELATED_ARPByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<Arps> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/AppendLine2`
   * @param {number} id1 - id1
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpAppendline2V1Post(
    id1: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpGetstocklinepriceV1(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpGetstocklinepriceV1Post(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpAddline2V1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpApplyaccdisttemplateV1(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpApplyaccdisttemplateV1Post(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEXRELATED_ARPNOTESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEXRELATED_ARPNOTESV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEXRELATED_ARPNOTESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsArpnotesxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEXRELATED_ARPNOTESByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsArpnotesxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesAppendline2V1Post(
    id1: number,
    id2: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpnotesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/NOTES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpnotesSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/NOTES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEXRELATED_ARPGENIUSFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEXRELATED_ARPGENIUSFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEXRELATED_ARPGENIUSFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEXRELATED_ARPGENIUSFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicexrelatedArpgeniusfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/XRELATED_ARP/{id2}/GENIUSFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicexrelatedArpgeniusfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/XRELATED_ARP/${id2}/GENIUSFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepreacclinesGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PREACCLINES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesGetstocklinepriceV1(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PREACCLINES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepreacclinesGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PREACCLINES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesGetstocklinepriceV1Get(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PREACCLINES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepreacclinesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PREACCLINES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PREACCLINES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepreacclinesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PREACCLINES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesAddline2V1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PREACCLINES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepreacclinesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PREACCLINES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PREACCLINES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepreacclinesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PREACCLINES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PREACCLINES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepreacclinesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PREACCLINES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PREACCLINES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepreacclinesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PREACCLINES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PREACCLINES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepreacclinesApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PREACCLINES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesApplyaccdisttemplateV1(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PREACCLINES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepreacclinesApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PREACCLINES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesApplyaccdisttemplateV1Post(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PREACCLINES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepreacclinesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PREACCLINES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PREACCLINES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepreacclinesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PREACCLINES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PREACCLINES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEOKCINFO_LISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEOKCINFO_LISTV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEOKCINFO_LISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsOkcinfoxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEOKCINFO_LISTByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsOkcinfoxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListAppendline2V1Post(
    id1: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListGetstocklinepriceV1(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListGetstocklinepriceV1Post(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListAddline2V1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListApplyaccdisttemplateV1(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListApplyaccdisttemplateV1Post(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceokcinfoListSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/OKCINFO_LIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceokcinfoListSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/OKCINFO_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICELABEL_LISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/LABEL_LIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICELABEL_LISTV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/LABEL_LIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICELABEL_LISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsLabelxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICELABEL_LISTByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsLabelxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicelabelListAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicelabelListAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListAppendline2V1Post(
    id1: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicelabelListAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicelabelListAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicelabelListGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListGetstocklinepriceV1(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicelabelListGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListGetstocklinepriceV1Post(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicelabelListAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicelabelListAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListAddline2V1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicelabelListAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicelabelListAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicelabelListEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicelabelListEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/LABEL_LIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicelabelListEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/LABEL_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistApplyaccdisttemplateV1(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDEFNFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDEFNFLDSLISTV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDEFNFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDEFNFLDSLISTByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistAppendline2V1Post(
    id1: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistGetstocklinepriceV1(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistGetstocklinepriceV1Post(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistAddline2V1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistApplyaccdisttemplateV1Post(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedefnfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedefnfldslistSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEINTEL_LISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/INTEL_LIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEINTEL_LISTV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/INTEL_LIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEINTEL_LISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsIntellistxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEINTEL_LISTByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsIntellistxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceintelListAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceintelListAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListAppendline2V1Post(
    id1: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceintelListAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceintelListAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceintelListGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListGetstocklinepriceV1(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceintelListGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListGetstocklinepriceV1Post(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceintelListAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceintelListAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListAddline2V1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceintelListAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceintelListAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceintelListEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceintelListEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceintelListApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListApplyaccdisttemplateV1(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceintelListApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListApplyaccdisttemplateV1Post(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceintelListSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceintelListSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/INTEL_LIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceintelListSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/INTEL_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEPREACCLINESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PREACCLINES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEPREACCLINESV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PREACCLINES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEPREACCLINESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PREACCLINES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsPreaccdistlnxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEPREACCLINESByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsPreaccdistlnxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PREACCLINES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepreacclinesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PREACCLINES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PREACCLINES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepreacclinesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PREACCLINES/AppendLine2`
   * @param {number} id1 - id1
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesAppendline2V1Post(
    id1: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PREACCLINES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepreacclinesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PREACCLINES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PREACCLINES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepreacclinesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PREACCLINES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepreacclinesAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PREACCLINES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepaymentListGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListGetstocklinepriceV1(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepaymentListGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListGetstocklinepriceV1Get(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListAddline2V1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepaymentListAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepaymentListEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepaymentListApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListApplyaccdisttemplateV1(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListApplyaccdisttemplateV1Post(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepaymentListSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEPAYMENT_LISTDISCTRLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEPAYMENT_LISTDISCTRLISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEPAYMENT_LISTDISCTRLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsDiscpaytrxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEPAYMENT_LISTDISCTRLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsDiscpaytrxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListdisctrlistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListdisctrlistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEGENIUSFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEGENIUSFLDSLISTV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEGENIUSFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEGENIUSFLDSLISTByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistAppendline2V1Post(
    id1: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistGetstocklinepriceV1(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistGetstocklinepriceV1Post(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistAddline2V1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicegeniusfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/GENIUSFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicegeniusfldslistEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/GENIUSFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEADDTAXLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEADDTAXLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEADDTAXLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsAddtaxmultixml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEADDTAXLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsAddtaxmultixml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceaddtaxlistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/ADDTAXLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceaddtaxlistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/ADDTAXLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_DISCEXPV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_DISCEXPV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_DISCEXPByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<PurchaseDiscounts>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_DISCEXPByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<PurchaseDiscounts> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedDiscexpSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_DISCEXP/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedDiscexpSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_DISCEXP/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEPAYMENT_LISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEPAYMENT_LISTV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEPAYMENT_LISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsPaylstxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEPAYMENT_LISTByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsPaylstxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListAppendline2V1Post(
    id1: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicepaymentListAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicepaymentListAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/PAYMENT_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicepaymentListAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/PAYMENT_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEUNITSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEUNITSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEUNITSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsServunitasgn>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEUNITSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsServunitasgn> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceunitsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/UNITS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceunitsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/UNITS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEGL_LINKSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEGL_LINKSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEGL_LINKSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsGlpostxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEGL_LINKSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsGlpostxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceglLinksEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/GL_LINKS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceglLinksEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/GL_LINKS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMLABEL_LISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMLABEL_LISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMLABEL_LISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsLabelxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMLABEL_LISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsLabelxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemlabelListSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/LABEL_LIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemlabelListSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/LABEL_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<PurchasedServices>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<PurchasedServices> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServiceSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServiceSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEWH_PARAMSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEWH_PARAMSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEWH_PARAMSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsSrvinvenxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_SERVICEWH_PARAMSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsSrvinvenxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedServicewhParamsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_SERVICE/{id3}/WH_PARAMS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedServicewhParamsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_SERVICE/${id3}/WH_PARAMS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSTEMPVALSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSTEMPVALSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSTEMPVALSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsVrntcdtempvaluex>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSTEMPVALSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsVrntcdtempvaluex> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{id4}/TEMPVALS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempstempvalsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${id4}/TEMPVALS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMALTERNATIVESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMALTERNATIVESV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMALTERNATIVESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ItemAlternatives>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMALTERNATIVESByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ItemAlternatives> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemalternativesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ALTERNATIVES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemalternativesEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ALTERNATIVES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTCODETEMPSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTCODETEMPSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTCODETEMPSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsVrntcdtemplatexm>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTCODETEMPSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsVrntcdtemplatexm> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTCODETEMPSTEMPVALSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTCODETEMPSTEMPVALSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTCODETEMPSTEMPVALSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsVrntcdtempvaluex>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTCODETEMPSTEMPVALSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsVrntcdtempvaluex> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTCODETEMPS/{id4}/TEMPVALS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntcodetempstempvalsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTCODETEMPS/${id4}/TEMPVALS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsVrntcdtemplatexm>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsVrntcdtemplatexm> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexcptempsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCPTEMPS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexcptempsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCPTEMPS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsExclistnodexml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsExclistnodexml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSVALREFSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSVALREFSLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSVALREFSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsExclistnodevalxm>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSVALREFSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsExclistnodevalxm> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/VRNTEXCEPTIONS/{id4}/VALREFSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemvrntexceptionsvalrefslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/VRNTEXCEPTIONS/${id4}/VALREFSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMQPRODSUBCONTSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMQPRODSUBCONTSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMQPRODSUBCONTSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemqprodxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMQPRODSUBCONTSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemqprodxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsubcontsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODSUBCONTS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsubcontsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODSUBCONTS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMGENIUSFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMGENIUSFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMGENIUSFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMGENIUSFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemgeniusfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GENIUSFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemgeniusfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GENIUSFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMDEFNFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMDEFNFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMDEFNFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMDEFNFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdefnfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdefnfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMADDTAXLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMADDTAXLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMADDTAXLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsAddtaxmultixml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMADDTAXLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsAddtaxmultixml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemaddtaxlistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/ADDTAXLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemaddtaxlistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/ADDTAXLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMQPRODSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMQPRODSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMQPRODSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemqprodxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMQPRODSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemqprodxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemqprodsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/QPRODS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemqprodsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/QPRODS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMCOMPOSITESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMCOMPOSITESV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMCOMPOSITESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemcmpxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMCOMPOSITESByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemcmpxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcompositesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/COMPOSITES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcompositesSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/COMPOSITES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMGL_LINKSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMGL_LINKSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMGL_LINKSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsGlpostxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMGL_LINKSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsGlpostxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemglLinksSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/GL_LINKS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemglLinksSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/GL_LINKS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMSUPPLIERSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMSUPPLIERSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMSUPPLIERSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemsuppxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMSUPPLIERSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemsuppxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemsuppliersAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/SUPPLIERS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemsuppliersAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/SUPPLIERS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMUNITSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMUNITSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMUNITSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemunitasgn>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMUNITSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemunitasgn> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMUNITSBARCODE_LISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMUNITSBARCODE_LISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMUNITSBARCODE_LISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsBarcodexml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMUNITSBARCODE_LISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsBarcodexml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemunitsbarcodeListEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/UNITS/{id4}/BARCODE_LIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemunitsbarcodeListEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/UNITS/${id4}/BARCODE_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMCHARACTERISTICSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMCHARACTERISTICSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMCHARACTERISTICSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemchcodesxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMCHARACTERISTICSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemchcodesxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMCHARACTERISTICSVALUESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMCHARACTERISTICSVALUESV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMCHARACTERISTICSVALUESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemchvaluesxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMCHARACTERISTICSVALUESByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemchvaluesxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/CHARACTERISTICS/{id4}/VALUES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemcharacteristicsvaluesSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/CHARACTERISTICS/${id4}/VALUES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMDOMINANT_CLASSESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMDOMINANT_CLASSESV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMDOMINANT_CLASSESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsXmldomcls>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMDOMINANT_CLASSESByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsXmldomcls> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemdominantClassesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/DOMINANT_CLASSES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemdominantClassesAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/DOMINANT_CLASSES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemGetstocklinepriceV1(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMFACTORY_PARAMSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMFACTORY_PARAMSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMFACTORY_PARAMSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemfactoryxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMFACTORY_PARAMSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemfactoryxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemfactoryParamsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/FACTORY_PARAMS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemfactoryParamsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/FACTORY_PARAMS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMWH_PARAMSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMWH_PARAMSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMWH_PARAMSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsIteminvenxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMWH_PARAMSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsIteminvenxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemwhParamsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{id3}/WH_PARAMS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemwhParamsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${id3}/WH_PARAMS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSFAREGDEFNFLDLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSFAREGDEFNFLDLISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSFAREGDEFNFLDLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSFAREGDEFNFLDLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaregdefnfldlistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FAREGDEFNFLDLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaregdefnfldlistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FAREGDEFNFLDLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSPREACCLINESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSPREACCLINESV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSPREACCLINESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsPreaccdistlnxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSPREACCLINESByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsPreaccdistlnxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionspreacclinesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/PREACCLINES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionspreacclinesSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/PREACCLINES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<Items>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSXRELATED_ITEMByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<Items> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsxrelatedItemAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/XRELATED_ITEM/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsxrelatedItemAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/XRELATED_ITEM/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistGetstocklinepriceV1(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSGENIUSFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSGENIUSFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSGENIUSFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSGENIUSFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsgeniusfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/GENIUSFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsgeniusfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/GENIUSFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSDEFNFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSDEFNFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSDEFNFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSDEFNFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdefnfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdefnfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSQCLISTVALLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSQCLISTVALLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSQCLISTVALLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsQccvallistxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSQCLISTVALLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsQccvallistxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistvallistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{id3}/VALLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistvallistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${id3}/VALLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSCAMPAIGN_INFOSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSCAMPAIGN_INFOSV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSCAMPAIGN_INFOSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsCampcodeslistxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSCAMPAIGN_INFOSByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsCampcodeslistxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionscampaignInfosSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/CAMPAIGN_INFOS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionscampaignInfosSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/CAMPAIGN_INFOS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSADDTAXLINELISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSADDTAXLINELISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSADDTAXLINELISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsAddtaxmultilinex>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSADDTAXLINELISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsAddtaxmultilinex> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsaddtaxlinelistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/ADDTAXLINELIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsaddtaxlinelistAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/ADDTAXLINELIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSDETAILSSL_DETAILSQCLISTVALLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSDETAILSSL_DETAILSQCLISTVALLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSDETAILSSL_DETAILSQCLISTVALLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsQccvallistxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSDETAILSSL_DETAILSQCLISTVALLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsQccvallistxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistvallistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistvallistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSQCLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSQCLISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSQCLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsQccvalentryxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSQCLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsQccvalentryxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsqclistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/QCLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsqclistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/QCLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSDETAILSFA_INFOV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSDETAILSFA_INFOV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSDETAILSFA_INFOByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsFaregtrnxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSDETAILSFA_INFOByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsFaregtrnxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsfaInfoSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/FA_INFO/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsfaInfoSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/FA_INFO/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSDETAILSSL_DETAILSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSDETAILSSL_DETAILSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSDETAILSSL_DETAILSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsSlloctrnxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSDETAILSSL_DETAILSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsSlloctrnxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSDETAILSSL_DETAILSQCLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSDETAILSSL_DETAILSQCLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSDETAILSSL_DETAILSQCLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsQccvalentryxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSDETAILSSL_DETAILSQCLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsQccvalentryxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsslDetailsqclistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{id3}/SL_DETAILS/{id4}/QCLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsslDetailsqclistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${id3}/SL_DETAILS/${id4}/QCLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSSL_DETAILSQCLISTVALLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSSL_DETAILSQCLISTVALLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSSL_DETAILSQCLISTVALLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsQccvallistxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSSL_DETAILSQCLISTVALLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsQccvallistxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistvallistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{id4}/VALLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistvallistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${id4}/VALLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSDETAILSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSDETAILSV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSDETAILSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsDetmattrxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSDETAILSByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsDetmattrxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsdetailsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/DETAILS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsdetailsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/DETAILS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsApplyaccdisttemplateV1(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSFA_INFOV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSFA_INFOV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSFA_INFOByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsFaregtrnxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSFA_INFOByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsFaregtrnxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsfaInfoSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/FA_INFO/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsfaInfoSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/FA_INFO/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSSL_DETAILSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSSL_DETAILSV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSSL_DETAILSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsSlloctrnxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSSL_DETAILSByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsSlloctrnxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSSL_DETAILSQCLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSSL_DETAILSQCLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSSL_DETAILSQCLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsQccvalentryxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSSL_DETAILSQCLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsQccvalentryxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsslDetailsqclistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{id2}/SL_DETAILS/{id3}/QCLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsslDetailsqclistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${id2}/SL_DETAILS/${id3}/QCLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPDEFNFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPDEFNFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPDEFNFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPDEFNFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpdefnfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpdefnfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICETRANSACTIONSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsDesptransxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICETRANSACTIONSByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsDesptransxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/AppendLine2`
   * @param {number} id1 - id1
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsAppendline2V1Post(
    id1: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsGetstocklinepriceV1(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsGetstocklinepriceV1Post(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsAddline2V1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicetransactionsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicetransactionsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/TRANSACTIONS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicetransactionsEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/TRANSACTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<Arps>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<Arps> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpAppendline2V1Post(
    id1: number,
    id2: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPNOTESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPNOTESV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPNOTESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsArpnotesxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPNOTESByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsArpnotesxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpnotesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/NOTES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpnotesSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/NOTES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPGENIUSFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPGENIUSFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPGENIUSFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESXRELATED_ARPGENIUSFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesxrelatedArpgeniusfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/XRELATED_ARP/{id3}/GENIUSFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Arps} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesxrelatedArpgeniusfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Arps,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/XRELATED_ARP/${id3}/GENIUSFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESDEFNFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESDEFNFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESDEFNFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESDEFNFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesdefnfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesdefnfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESLABEL_LISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESLABEL_LISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESLABEL_LISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsLabelxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESLABEL_LISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsLabelxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListAppendline2V1Post(
    id1: number,
    id2: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatcheslabelListEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/LABEL_LIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatcheslabelListEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/LABEL_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEGL_LINKSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEGL_LINKSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEGL_LINKSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsGlpostxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEGL_LINKSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsGlpostxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceglLinksSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/GL_LINKS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceglLinksSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/GL_LINKS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEADDTAXLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEADDTAXLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEADDTAXLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsAddtaxmultixml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEADDTAXLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsAddtaxmultixml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/ADDTAXLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceaddtaxlistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/ADDTAXLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_DISCEXPV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_DISCEXPV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_DISCEXPByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<PurchaseDiscounts>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_DISCEXPByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<PurchaseDiscounts> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedDiscexpAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_DISCEXP/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedDiscexpAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_DISCEXP/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEWH_PARAMSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEWH_PARAMSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEWH_PARAMSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsSrvinvenxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEWH_PARAMSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsSrvinvenxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServicewhParamsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/WH_PARAMS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServicewhParamsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/WH_PARAMS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEUNITSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEUNITSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEUNITSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsServunitasgn>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEUNITSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsServunitasgn> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceunitsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{id4}/UNITS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {PurchasedServices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceunitsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: PurchasedServices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${id4}/UNITS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMALTERNATIVESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMALTERNATIVESV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMALTERNATIVESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ItemAlternatives>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMALTERNATIVESByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ItemAlternatives> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemalternativesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ALTERNATIVES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemalternativesSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ALTERNATIVES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMLABEL_LISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMLABEL_LISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMLABEL_LISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsLabelxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMLABEL_LISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsLabelxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemlabelListSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/LABEL_LIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemlabelListSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/LABEL_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<PurchasedServices>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_SERVICEByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<PurchasedServices> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedServiceAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_SERVICE/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedServiceAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_SERVICE/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsVrntcdtemplatexm>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsVrntcdtemplatexm> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSTEMPVALSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSTEMPVALSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSTEMPVALSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsVrntcdtempvaluex>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCPTEMPSTEMPVALSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsVrntcdtempvaluex> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCPTEMPS/{id5}/TEMPVALS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexcptempstempvalsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCPTEMPS/${id5}/TEMPVALS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSVALREFSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSVALREFSLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSVALREFSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsExclistnodevalxm>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSVALREFSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsExclistnodevalxm> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{id5}/VALREFSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {RsExclistnodexml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsvalrefslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    data: RsExclistnodexml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${id5}/VALREFSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTCODETEMPSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTCODETEMPSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTCODETEMPSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsVrntcdtemplatexm>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTCODETEMPSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsVrntcdtemplatexm> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTCODETEMPSTEMPVALSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTCODETEMPSTEMPVALSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTCODETEMPSTEMPVALSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsVrntcdtempvaluex>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTCODETEMPSTEMPVALSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsVrntcdtempvaluex> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTCODETEMPS/{id5}/TEMPVALS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsVrntcdtemplatexm} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntcodetempstempvalsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsVrntcdtemplatexm,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTCODETEMPS/${id5}/TEMPVALS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMDEFNFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMDEFNFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMDEFNFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMDEFNFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdefnfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsExclistnodexml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMVRNTEXCEPTIONSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsExclistnodexml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/VRNTEXCEPTIONS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemvrntexceptionsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/VRNTEXCEPTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMQPRODSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMQPRODSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMQPRODSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemqprodxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMQPRODSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemqprodxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMQPRODSUBCONTSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMQPRODSUBCONTSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMQPRODSUBCONTSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemqprodxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMQPRODSUBCONTSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemqprodxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/QPRODSUBCONTS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemqprodsubcontsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/QPRODSUBCONTS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMGENIUSFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMGENIUSFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMGENIUSFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMGENIUSFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GENIUSFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemgeniusfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GENIUSFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMSUPPLIERSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMSUPPLIERSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMSUPPLIERSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemsuppxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMSUPPLIERSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemsuppxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemsuppliersSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/SUPPLIERS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemsuppliersSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/SUPPLIERS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMADDTAXLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMADDTAXLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMADDTAXLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsAddtaxmultixml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMADDTAXLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsAddtaxmultixml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/ADDTAXLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemaddtaxlistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/ADDTAXLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMUNITSBARCODE_LISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMUNITSBARCODE_LISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMUNITSBARCODE_LISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsBarcodexml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMUNITSBARCODE_LISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsBarcodexml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{id5}/BARCODE_LIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {RsItemunitasgn} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsbarcodeListSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    data: RsItemunitasgn,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${id5}/BARCODE_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMCOMPOSITESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMCOMPOSITESV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMCOMPOSITESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemcmpxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMCOMPOSITESByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemcmpxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcompositesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/COMPOSITES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcompositesSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/COMPOSITES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMGL_LINKSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMGL_LINKSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMGL_LINKSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsGlpostxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMGL_LINKSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsGlpostxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemglLinksAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/GL_LINKS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemglLinksAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/GL_LINKS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMDOMINANT_CLASSESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMDOMINANT_CLASSESV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMDOMINANT_CLASSESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsXmldomcls>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMDOMINANT_CLASSESByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsXmldomcls> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/DOMINANT_CLASSES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemdominantClassesSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/DOMINANT_CLASSES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMUNITSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMUNITSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMUNITSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemunitasgn>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMUNITSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemunitasgn> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemunitsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/UNITS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemunitsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/UNITS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMWH_PARAMSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMWH_PARAMSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMWH_PARAMSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsIteminvenxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMWH_PARAMSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsIteminvenxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemwhParamsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/WH_PARAMS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemwhParamsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/WH_PARAMS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMCHARACTERISTICSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMCHARACTERISTICSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMCHARACTERISTICSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemchcodesxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMCHARACTERISTICSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemchcodesxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMCHARACTERISTICSVALUESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMCHARACTERISTICSVALUESV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMCHARACTERISTICSVALUESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemchvaluesxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMCHARACTERISTICSVALUESByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemchvaluesxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/CHARACTERISTICS/{id5}/VALUES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsItemchcodesxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemcharacteristicsvaluesAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsItemchcodesxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/CHARACTERISTICS/${id5}/VALUES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<Items>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<Items> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMFACTORY_PARAMSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMFACTORY_PARAMSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMFACTORY_PARAMSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsItemfactoryxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSXRELATED_ITEMFACTORY_PARAMSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsItemfactoryxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/XRELATED_ITEM/{id4}/FACTORY_PARAMS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {Items} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsxrelatedItemfactoryParamsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: Items,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/XRELATED_ITEM/${id4}/FACTORY_PARAMS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDEFNFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDEFNFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDEFNFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDEFNFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdefnfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdefnfldslistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSFAREGDEFNFLDLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSFAREGDEFNFLDLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSFAREGDEFNFLDLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSFAREGDEFNFLDLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaregdefnfldlistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FAREGDEFNFLDLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaregdefnfldlistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FAREGDEFNFLDLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSPREACCLINESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSPREACCLINESV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSPREACCLINESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsPreaccdistlnxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSPREACCLINESByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsPreaccdistlnxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionspreacclinesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/PREACCLINES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionspreacclinesAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/PREACCLINES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSADDTAXLINELISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSADDTAXLINELISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSADDTAXLINELISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsAddtaxmultilinex>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSADDTAXLINELISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsAddtaxmultilinex> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsaddtaxlinelistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/ADDTAXLINELIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsaddtaxlinelistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/ADDTAXLINELIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSGENIUSFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSGENIUSFLDSLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSGENIUSFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSGENIUSFLDSLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsgeniusfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/GENIUSFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsgeniusfldslistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/GENIUSFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {number} Indx - _Indx
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    Indx: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSQCLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSQCLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSQCLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsQccvalentryxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSQCLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsQccvalentryxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSQCLISTVALLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSQCLISTVALLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSQCLISTVALLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsQccvallistxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSQCLISTVALLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsQccvallistxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsqclistvallistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/QCLIST/{id4}/VALLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsqclistvallistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/QCLIST/${id4}/VALLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSCAMPAIGN_INFOSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSCAMPAIGN_INFOSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSCAMPAIGN_INFOSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsCampcodeslistxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSCAMPAIGN_INFOSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsCampcodeslistxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionscampaignInfosAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/CAMPAIGN_INFOS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionscampaignInfosAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/CAMPAIGN_INFOS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSSL_DETAILSQCLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSSL_DETAILSQCLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSSL_DETAILSQCLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsQccvalentryxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSSL_DETAILSQCLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsQccvalentryxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSSL_DETAILSQCLISTVALLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSSL_DETAILSQCLISTVALLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSSL_DETAILSQCLISTVALLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsQccvallistxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSSL_DETAILSQCLISTVALLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsQccvallistxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    prcTyp: number,
    Indx: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {number} index - _index
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    index: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{id5}/QCLIST/{id6}/VALLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} id6 - id6
   * @param {number} index - _index
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsqclistvallistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    id6: number,
    index: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${id5}/QCLIST/${id6}/VALLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistApplyaccdisttemplateV1(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} Indx - _Indx
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    Indx: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsDetmattrxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsDetmattrxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSFA_INFOV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSFA_INFOV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSFA_INFOByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsFaregtrnxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSFA_INFOByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsFaregtrnxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsfaInfoSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/FA_INFO/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsfaInfoSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/FA_INFO/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSSL_DETAILSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSSL_DETAILSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSSL_DETAILSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsSlloctrnxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSDETAILSSL_DETAILSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsSlloctrnxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsdetailsslDetailsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/DETAILS/{id4}/SL_DETAILS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDetmattrxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsdetailsslDetailsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDetmattrxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/DETAILS/${id4}/SL_DETAILS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsGetstocklinepriceV1(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsGetstocklinepriceV1Get(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSSL_DETAILSQCLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSSL_DETAILSQCLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSSL_DETAILSQCLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsQccvalentryxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSSL_DETAILSQCLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsQccvalentryxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    prcTyp: number,
    Indx: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} index - _index
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    index: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} Indx - _Indx
   * @param {RsSlloctrnxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    Indx: number,
    data: RsSlloctrnxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSSL_DETAILSQCLISTVALLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSSL_DETAILSQCLISTVALLISTV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSSL_DETAILSQCLISTVALLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsQccvallistxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSSL_DETAILSQCLISTVALLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsQccvallistxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    prcTyp: number,
    Indx: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsqclistvallistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{id4}/QCLIST/{id5}/VALLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} id4 - id4
   * @param {number} id5 - id5
   * @param {number} index - _index
   * @param {RsQccvalentryxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsqclistvallistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    id4: number,
    id5: number,
    index: number,
    data: RsQccvalentryxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${id4}/QCLIST/${id5}/VALLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesApplyaccdisttemplateV1(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsDesptransxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsDesptransxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsAppendline2V1Post(
    id1: number,
    id2: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {PurchaseDispatches} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: PurchaseDispatches,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSFA_INFOV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSFA_INFOV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSFA_INFOByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsFaregtrnxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSFA_INFOByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsFaregtrnxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    id3: number,
    prcTyp: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoAddline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoAddline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoAppendserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} index - _index
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoEqualizebalanceV1Post(
    id1: number,
    id2: number,
    id3: number,
    index: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    id3: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsfaInfoSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/FA_INFO/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} Indx - _Indx
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsfaInfoSetlinetotalsV1Post(
    id1: number,
    id2: number,
    id3: number,
    Indx: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/FA_INFO/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSSL_DETAILSV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSSL_DETAILSV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSSL_DETAILSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsSlloctrnxml>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESTRANSACTIONSSL_DETAILSByIdV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsSlloctrnxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsAppendline2V1Post(
    id1: number,
    id2: number,
    id3: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchestransactionsslDetailsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{id2}/TRANSACTIONS/{id3}/SL_DETAILS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} id3 - id3
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsDesptransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchestransactionsslDetailsAddserilotsV1Post(
    id1: number,
    id2: number,
    id3: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsDesptransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${id2}/TRANSACTIONS/${id3}/SL_DETAILS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspreacclinesGetstocklinepriceV1
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PREACCLINES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesGetstocklinepriceV1(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PREACCLINES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspreacclinesGetstocklinepriceV1Get
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PREACCLINES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesGetstocklinepriceV1Get(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PREACCLINES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspreacclinesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PREACCLINES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PREACCLINES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspreacclinesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PREACCLINES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesAddline2V1Post(
    id1: number,
    index: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PREACCLINES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspreacclinesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PREACCLINES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PREACCLINES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspreacclinesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PREACCLINES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PREACCLINES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspreacclinesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PREACCLINES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PREACCLINES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspreacclinesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PREACCLINES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PREACCLINES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspreacclinesApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PREACCLINES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesApplyaccdisttemplateV1(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PREACCLINES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspreacclinesApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PREACCLINES/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesApplyaccdisttemplateV1Post(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PREACCLINES/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspreacclinesSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PREACCLINES/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PREACCLINES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspreacclinesSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PREACCLINES/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PREACCLINES/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEV1(id: number, options?: BankSlipsQueryOptions): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{idt}`
   * @param {number} id - id
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<PurchaseInvoices>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEByIdV1(
    id: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<PurchaseInvoices> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/AppendLine2`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceAppendline2V1(
    id: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/AppendLine2`
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceAppendline2V1Post(
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceAddserilotsV1(
    id: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceAddserilotsV1Post(
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceGetstocklinepriceV1(
    id: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceGetstocklinepriceV1Post(
    prcTyp: number,
    Indx: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceAddline2V1(
    id: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/AddLine2/{_index}`
   * @param {number} index - _index
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceAddline2V1Post(
    index: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceAppendserilotsV1(
    id: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceAppendserilotsV1Post(
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceEqualizebalanceV1(
    id: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/EqualizeBalance/{_index}`
   * @param {number} index - _index
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceEqualizebalanceV1Post(
    index: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceApplyaccdisttemplateV1(
    id: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceApplyaccdisttemplateV1Post(
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoiceSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceSetlinetotalsV1(
    id: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoiceSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/SetLineTotals/{_Indx}`
   * @param {number} Indx - _Indx
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoiceSetlinetotalsV1Post(
    Indx: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getATTACHMENT_INVOICEDISPATCHESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<PurchaseDispatches>} A promise resolving to the response.
   */
  async getATTACHMENT_INVOICEDISPATCHESByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<PurchaseDispatches> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/AppendLine2`
   * @param {number} id1 - id1
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesAppendline2V1Post(
    id1: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesGetstocklinepriceV1(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesGetstocklinepriceV1Post(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesAddline2V1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesAddline2V1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method attachmentInvoicedispatchesEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/ATTACHMENT_INVOICE/{id1}/DISPATCHES/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/ATTACHMENT_INVOICE/${id1}/DISPATCHES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method attachmentInvoicedispatchesEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/ATTACHMENT_INVOICE/{id1}/DISPATCHES/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {PurchaseInvoices} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async attachmentInvoicedispatchesEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: PurchaseInvoices,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/ATTACHMENT_INVOICE/${id1}/DISPATCHES/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspaymentListApplyaccdisttemplateV1
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListApplyaccdisttemplateV1(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspaymentListApplyaccdisttemplateV1Get
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListApplyaccdisttemplateV1Get(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getTRANSACTIONSPAYMENT_LISTDISCTRLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getTRANSACTIONSPAYMENT_LISTDISCTRLISTV1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getTRANSACTIONSPAYMENT_LISTDISCTRLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsDiscpaytrxml>} A promise resolving to the response.
   */
  async getTRANSACTIONSPAYMENT_LISTDISCTRLISTByIdV1(
    id: number,
    id1: number,
    id2: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsDiscpaytrxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListdisctrlistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistAppendline2V1(
    id: number,
    id1: number,
    id2: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListdisctrlistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistAppendline2V1Post(
    id1: number,
    id2: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspaymentListdisctrlistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistAddserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListdisctrlistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistAddserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspaymentListdisctrlistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistGetstocklinepriceV1(
    id: number,
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListdisctrlistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistGetstocklinepriceV1Post(
    id1: number,
    id2: number,
    prcTyp: number,
    Indx: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspaymentListdisctrlistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistAddline2V1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListdisctrlistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistAddline2V1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspaymentListdisctrlistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistAppendserilotsV1(
    id: number,
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListdisctrlistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistAppendserilotsV1Post(
    id1: number,
    id2: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspaymentListdisctrlistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistEqualizebalanceV1(
    id: number,
    id1: number,
    id2: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListdisctrlistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} index - _index
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistEqualizebalanceV1Post(
    id1: number,
    id2: number,
    index: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspaymentListdisctrlistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListdisctrlistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistApplyaccdisttemplateV1Post(
    id1: number,
    id2: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspaymentListdisctrlistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistSetlinetotalsV1(
    id: number,
    id1: number,
    id2: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListdisctrlistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/{id2}/DISCTRLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} id2 - id2
   * @param {number} Indx - _Indx
   * @param {RsPaylstxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListdisctrlistSetlinetotalsV1Post(
    id1: number,
    id2: number,
    Indx: number,
    data: RsPaylstxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/${id2}/DISCTRLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getTRANSACTIONSDEFNFLDSLISTV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/DEFNFLDSLIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getTRANSACTIONSDEFNFLDSLISTV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/DEFNFLDSLIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getTRANSACTIONSDEFNFLDSLISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/DEFNFLDSLIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ExtendedFieldDefinitions>} A promise resolving to the response.
   */
  async getTRANSACTIONSDEFNFLDSLISTByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<ExtendedFieldDefinitions> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/DEFNFLDSLIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsdefnfldslistAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsdefnfldslistAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/DEFNFLDSLIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistAppendline2V1Post(
    id1: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/DEFNFLDSLIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionsdefnfldslistAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsdefnfldslistAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/DEFNFLDSLIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/DEFNFLDSLIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionsdefnfldslistGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistGetstocklinepriceV1(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsdefnfldslistGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/DEFNFLDSLIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistGetstocklinepriceV1Post(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/DEFNFLDSLIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionsdefnfldslistAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsdefnfldslistAddline2V1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/DEFNFLDSLIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistAddline2V1Post(
    id1: number,
    index: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/DEFNFLDSLIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionsdefnfldslistAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsdefnfldslistAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/DEFNFLDSLIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/DEFNFLDSLIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionsdefnfldslistEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsdefnfldslistEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/DEFNFLDSLIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/DEFNFLDSLIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionsdefnfldslistApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistApplyaccdisttemplateV1(
    id: number,
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsdefnfldslistApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/DEFNFLDSLIST/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id1 - id1
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistApplyaccdisttemplateV1Post(
    id1: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/DEFNFLDSLIST/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionsdefnfldslistSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistSetlinetotalsV1(
    id: number,
    id1: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsdefnfldslistSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/DEFNFLDSLIST/SetLineTotals/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} Indx - _Indx
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsdefnfldslistSetlinetotalsV1Post(
    id1: number,
    Indx: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/DEFNFLDSLIST/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getTRANSACTIONSPREACCLINESV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PREACCLINES`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getTRANSACTIONSPREACCLINESV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PREACCLINES${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getTRANSACTIONSPREACCLINESByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PREACCLINES/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsPreaccdistlnxml>} A promise resolving to the response.
   */
  async getTRANSACTIONSPREACCLINESByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsPreaccdistlnxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PREACCLINES/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspreacclinesAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PREACCLINES/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PREACCLINES/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspreacclinesAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PREACCLINES/AppendLine2`
   * @param {number} id1 - id1
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesAppendline2V1Post(
    id1: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PREACCLINES/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspreacclinesAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PREACCLINES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PREACCLINES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspreacclinesAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PREACCLINES/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspreacclinesAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PREACCLINES/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method postReadWithBodyParametersV1
   * @description Corresponds to `POST /bankSlips/readwithbodyparamters`
   * @param {RequestQueryBodyParameter} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<BankSlips>} A promise resolving to the response.
   */
  async postReadWithBodyParametersV1(
    data: RequestQueryBodyParameter,
    options?: BankSlipsQueryOptions
  ): Promise<BankSlips> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/readwithbodyparamters${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method postReadByIdWithBodyParametersV1
   * @description Corresponds to `POST /bankSlips/{id}/readwithbodyparamters`
   * @param {number} id - id
   * @param {RequestQueryBodyParameter} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<BankSlips>} A promise resolving to the response.
   */
  async postReadByIdWithBodyParametersV1(
    id: number,
    data: RequestQueryBodyParameter,
    options?: BankSlipsQueryOptions
  ): Promise<BankSlips> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/${id}/readwithbodyparamters${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getTRANSACTIONSV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getTRANSACTIONSV1(id: number, options?: BankSlipsQueryOptions): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getTRANSACTIONSByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{idt}`
   * @param {number} id - id
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsBanktransxml>} A promise resolving to the response.
   */
  async getTRANSACTIONSByIdV1(
    id: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsBanktransxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/AppendLine2`
   * @param {number} id - id
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsAppendline2V1(
    id: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/AppendLine2`
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsAppendline2V1Post(
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionsAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsAddserilotsV1(
    id: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsAddserilotsV1Post(
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionsGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsGetstocklinepriceV1(
    id: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsGetstocklinepriceV1Post(
    prcTyp: number,
    Indx: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionsAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsAddline2V1(
    id: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsAddline2V1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/AddLine2/{_index}`
   * @param {number} index - _index
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsAddline2V1Post(
    index: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionsAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsAppendserilotsV1(
    id: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsAppendserilotsV1Post(
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionsEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsEqualizebalanceV1(
    id: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/EqualizeBalance/{_index}`
   * @param {number} index - _index
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsEqualizebalanceV1Post(
    index: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionsApplyaccdisttemplateV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {number} id - id
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsApplyaccdisttemplateV1(
    id: number,
    accDistCode: string,
    lineType: number,
    iDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsApplyaccdisttemplateV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/ApplyAccDistTemplate/{_accDistCode}/{_lineType}/{_iDate}`
   * @param {string} accDistCode - _accDistCode
   * @param {number} lineType - _lineType
   * @param {number} iDate - _iDate
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsApplyaccdisttemplateV1Post(
    accDistCode: string,
    lineType: number,
    iDate: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/ApplyAccDistTemplate/${accDistCode}/${lineType}/${iDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionsSetlinetotalsV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/SetLineTotals/{_Indx}`
   * @param {number} id - id
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsSetlinetotalsV1(
    id: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionsSetlinetotalsV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/SetLineTotals/{_Indx}`
   * @param {number} Indx - _Indx
   * @param {Partial<BankSlips>} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionsSetlinetotalsV1Post(
    Indx: number,
    data: Partial<BankSlips>,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/SetLineTotals/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method getTRANSACTIONSPAYMENT_LISTV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<ResultData>} A promise resolving to the response.
   */
  async getTRANSACTIONSPAYMENT_LISTV1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<ResultData> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method getTRANSACTIONSPAYMENT_LISTByIdV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/{idt}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} idt - idt
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<RsPaylstxml>} A promise resolving to the response.
   */
  async getTRANSACTIONSPAYMENT_LISTByIdV1(
    id: number,
    id1: number,
    idt: number,
    options?: BankSlipsQueryOptions
  ): Promise<RsPaylstxml> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/${idt}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListAppendline2V1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/AppendLine2`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListAppendline2V1(
    id: number,
    id1: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListAppendline2V1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/AppendLine2`
   * @param {number} id1 - id1
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListAppendline2V1Post(
    id1: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/AppendLine2${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspaymentListAddserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListAddserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListAddserilotsV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/AddSeriLots/{_slCode}/{_amount}/{_locCode}/{_lOMtd}/{_ExpDate}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {number} ExpDate - _ExpDate
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListAddserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    locCode: string,
    lOMtd: number,
    ExpDate: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/AddSeriLots/${slCode}/${amount}/${locCode}/${lOMtd}/${ExpDate}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspaymentListGetstocklinepriceV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListGetstocklinepriceV1(
    id: number,
    id1: number,
    prcTyp: number,
    Indx: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListGetstocklinepriceV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/GetStockLinePrice/{_prcTyp}/{_Indx}`
   * @param {number} id1 - id1
   * @param {number} prcTyp - _prcTyp
   * @param {number} Indx - _Indx
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListGetstocklinepriceV1Post(
    id1: number,
    prcTyp: number,
    Indx: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/GetStockLinePrice/${prcTyp}/${Indx}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspaymentListAddline2V1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/AddLine2/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListAddline2V1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListAddline2V1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/AddLine2/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListAddline2V1Post(
    id1: number,
    index: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/AddLine2/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspaymentListAppendserilotsV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListAppendserilotsV1(
    id: number,
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListAppendserilotsV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/AppendSeriLots/{_slCode}/{_amount}/{_Indx}/{_locCode}/{_lOMtd}`
   * @param {number} id1 - id1
   * @param {string} slCode - _slCode
   * @param {number} amount - _amount
   * @param {number} Indx - _Indx
   * @param {string} locCode - _locCode
   * @param {number} lOMtd - _lOMtd
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListAppendserilotsV1Post(
    id1: number,
    slCode: string,
    amount: number,
    Indx: number,
    locCode: string,
    lOMtd: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/AppendSeriLots/${slCode}/${amount}/${Indx}/${locCode}/${lOMtd}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * @method transactionspaymentListEqualizebalanceV1
   * @description Corresponds to `GET /bankSlips/{id}/TRANSACTIONS/{id1}/PAYMENT_LIST/EqualizeBalance/{_index}`
   * @param {number} id - id
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListEqualizebalanceV1(
    id: number,
    id1: number,
    index: number,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'get',
      `/bankSlips/${id}/TRANSACTIONS/${id1}/PAYMENT_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`
    );
  }

  /**
   * @method transactionspaymentListEqualizebalanceV1Post
   * @description Corresponds to `POST /bankSlips/TRANSACTIONS/{id1}/PAYMENT_LIST/EqualizeBalance/{_index}`
   * @param {number} id1 - id1
   * @param {number} index - _index
   * @param {RsBanktransxml} data - The request body.
   * @param {QueryOptions} [options] - Optional query parameters.
   * @returns {Promise<KeyValueParameter[]>} A promise resolving to the response.
   */
  async transactionspaymentListEqualizebalanceV1Post(
    id1: number,
    index: number,
    data: RsBanktransxml,
    options?: BankSlipsQueryOptions
  ): Promise<KeyValueParameter[]> {
    const queryString = options ? this.buildQueryString(options) : '';
    return this.request(
      'post',
      `/bankSlips/TRANSACTIONS/${id1}/PAYMENT_LIST/EqualizeBalance/${index}${queryString ? `?${queryString}` : ''}`,
      data
    );
  }

  /**
   * Search bankSlips by AUXIL_CODE
   * @param auxilCode - The AUXIL_CODE to search for
   * @returns Promise resolving to matching entities
   */
  async searchByAuxilCode(auxilCode: string | number | boolean): Promise<ApiResponse<BankSlips>> {
    return this.getAll({ q: `AUXIL_CODE like '${auxilCode}*'` });
  }

  /**
   * Search bankSlips by NUMBER
   * @param number - The NUMBER to search for
   * @returns Promise resolving to matching entities
   */
  async searchBynumber(number: string | number | boolean): Promise<ApiResponse<BankSlips>> {
    return this.getAll({ q: `NUMBER eq ${number}` });
  }

  /**
   * Search bankSlips by TYPE
   * @param type - The TYPE to search for
   * @returns Promise resolving to matching entities
   */
  async searchByType(type: string | number | boolean): Promise<ApiResponse<BankSlips>> {
    return this.getAll({ q: `TYPE eq ${type}` });
  }

  /**
   * @method getAnalytics
   * @description Retrieves analytics data for `BankSlips` entities.
   * @returns {Promise<BankSlipsAnalytics>} A promise that resolves to the analytics data.
   *
   * @example
   * // Retrieves analytics for BankSlips
   * const analytics = await client.bankSlips.getAnalytics();
   */
  async getAnalytics(): Promise<BankSlipsAnalytics> {
    const count = await this.getCount();
    return {
      total: count,
      // Add more analytics as needed
    };
  }

  /**
   * @method getCount
   * @description Retrieves the total count of `BankSlips` entities.
   * @returns {Promise<number>} A promise that resolves to the total count.
   *
   * @example
   * // Retrieves the total count of BankSlips
   * const count = await client.bankSlips.getCount();
   */
  async getCount(): Promise<number> {
    const response = await this.getAll({ count: true });
    return response.totalCount || 0;
  }

  /**
   * @method getDbColumns
   * @description Retrieves database column information for the `BankSlips` entity.
   * @returns {Promise<unknown[]>} A promise that resolves to an array of column definitions.
   *
   * @example
   * // Retrieves column info for BankSlips
   * const columns = await client.bankSlips.getDbColumns();
   */
  async getDbColumns(): Promise<unknown[]> {
    return this.request('get', `${this.endpoint}/dbcolumns`);
  }

  /**
   * @method track
   * @description Enables change tracking for the `BankSlips` entity.
   * @returns {Promise<unknown>} A promise that resolves with the tracking status.
   *
   * @example
   * // Enables change tracking for BankSlips
   * await client.bankSlips.track();
   */
  async track(): Promise<unknown> {
    return this.request('get', `${this.endpoint}/track`);
  }

  /**
   * @method untrack
   * @description Disables change tracking for the `BankSlips` entity.
   * @returns {Promise<unknown>} A promise that resolves with the tracking status.
   *
   * @example
   * // Disables change tracking for BankSlips
   * await client.bankSlips.untrack();
   */
  async untrack(): Promise<unknown> {
    return this.request('get', `${this.endpoint}/untrack`);
  }

  /**
   * @method checkTrackStatus
   * @description Checks the change tracking status for the `BankSlips` entity.
   * @returns {Promise<boolean>} A promise that resolves to `true` if tracking is enabled, otherwise `false`.
   *
   * @example
   * // Checks the tracking status for BankSlips
   * const isTracking = await client.bankSlips.checkTrackStatus();
   */
  async checkTrackStatus(): Promise<boolean> {
    return this.request('get', `${this.endpoint}/checktrack`);
  }

  /**
   * @method buildSearchQuery
   * @private
   * @description Builds a search query string from a criteria object.
   * @param {BankSlipsSearchCriteria} criteria - The search criteria.
   * @returns {string | undefined} The OData query string, or `undefined` if no criteria are provided.
   */
  private buildSearchQuery(criteria: BankSlipsSearchCriteria): string | undefined {
    return buildSearchQuery(criteria);
  }
}
