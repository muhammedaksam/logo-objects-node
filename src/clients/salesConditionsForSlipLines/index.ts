/**
 * SalesConditionsForSlipLines module - provides SalesConditionsForSlipLines entity types and client
 */

// Export types (excluding imported operator types to avoid re-export conflicts)
export type {
  SalesConditionsForSlipLines,
  SalesConditionsForSlipLinesSearchCriteria,
  SalesConditionsForSlipLinesAnalytics,
  SalesConditionsForSlipLinesQueryOptions,
} from './types';

// Export client
export { SalesConditionsForSlipLinesClient } from './client';
